%h1 New hotel

= semantic_form_for @hotel, :url => user_hotels_path(@owner) do |f|
  = render :partial => 'form_content', :locals => {:f => f}

= render 'google_map'
= link_to 'Back', user_hotels_path(@owner)
