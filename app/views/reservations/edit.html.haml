%h1 Editing reservation

= semantic_form_for @reservation, :url => room_reservation_path(@room, @reservation) do |f|
  = render 'form', :f => f

= link_to 'Show', room_reservation_path(@room, @reservation)
\|
= link_to 'Back', room_reservations_path(@room)
