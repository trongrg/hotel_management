= f.inputs do
  = f.input :name
  = f.input :price
  = f.input :currency, :as => :select, :collection => RoomType::CURRENCIES, :include_blank => false
  = f.input :description
  = f.input :image

= f.actions do
  = f.action(:submit)
